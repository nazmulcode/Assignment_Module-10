function calculateDifference(a, b) {
  return a - b;
}

// console.log(calculateDifference(10, 5));

// 2nd
function isOdd(number) {
  if (number % 2 === 0) {
    return false;
  } else {
    return true;
  }
}

console.log(isOdd(5));
